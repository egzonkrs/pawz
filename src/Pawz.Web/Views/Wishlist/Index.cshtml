@model Wishlist

@{
    ViewBag.Title = "Wish list";
    Layout = "_Layout";
}

<h2>Your Wishlist</h2>

@if (Model != null && Model.Pets.Any())
{
    <ul>
        @foreach (var pet in Model.Pets)
        {
            <li>
                <strong>@pet.Name</strong> - @pet.About
                <form asp-action="RemovePetFromWishlist" method="post">
                    <input type="hidden" name="petId" value="@pet.Id" />
                    <input type="submit" value="Remove" class="btn btn-danger" />
                </form>
            </li>
        }
    </ul>
}
else
{
    <p>Your wishlist is empty.</p>
}

@* <form asp-action="AddPetToWishlist" method="post"> *@
@*     <div> *@
@*         <label for="petId">Pet ID:</label> *@
@*         <input type="number" id="petId" name="petId" /> *@
@*     </div> *@
@*     <div> *@
@*         <input type="submit" value="Add Pet" class="btn btn-primary" /> *@
@*     </div> *@
@* </form> *@

<form asp-action="ClearWishlist" method="post">
    <input type="hidden" name="Id" value="@Model.UserId" />
    <input type="submit" value="Clear Wishlist" class="btn btn-warning" />
</form>
